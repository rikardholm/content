version = '1.0'

subprojects {
    apply plugin: 'java'
    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}

project(':content-alternative-implementations') {
   dependencies {
       compile project(':content-api')
   }
}